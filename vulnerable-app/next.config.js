/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a vulnerable application for testing purposes
  // DO NOT deploy this configuration to production
}

module.exports = nextConfig
